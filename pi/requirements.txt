# AgriRover Raspberry Pi dependencies (BOM Layer 12).
# Pin major versions to the stack documented in docs/circuit-diagram.md.

# --- Vision / AI ---
opencv-python==4.8.*
tflite-runtime==2.14.*
ultralytics            # YOLOv8n (obstacle + weed detection)
# pycoral             # install from Coral's apt/whl for the Edge TPU delegate

# --- Messaging / IO ---
paho-mqtt==1.6.*
pyserial==3.5
RPi.GPIO              # encoders, float, rain, buttons, mode selector
smbus2               # I2C: INA219, MPU6050, VL53L1X, OLED, PCF8574

# --- Stream processing + dashboard ---
pathway
streamlit==1.28.*
folium
pydeck

# --- Alerts ---
requests             # Telegram Bot API
